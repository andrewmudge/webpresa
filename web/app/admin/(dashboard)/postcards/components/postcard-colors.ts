/**
 * Postcard-specific brand blue (2026-08-06, user-specified) — deliberately
 * not the sitewide `--color-brand` token (`#11455E`): the postcard's own
 * design calls for a brighter, more saturated blue than the rest of the
 * admin/marketing app uses, so this is scoped to the postcard components
 * only rather than changing the global token.
 */
export const POSTCARD_BLUE = '#0048AA';
