/**
 * Postcard-specific palette (2026-08-06, user-specified) — deliberately not
 * the sitewide `--color-brand` token (`#11455E`): the postcard's own design
 * calls for a brighter, more saturated blue family than the rest of the
 * admin/marketing app uses, so this is scoped to the postcard components
 * only rather than changing the global token.
 */
export const POSTCARD_NAVY = '#0B1E3D';
export const POSTCARD_BLUE = '#0048AA';
export const POSTCARD_BLUE_LIGHT = '#2170E8';
