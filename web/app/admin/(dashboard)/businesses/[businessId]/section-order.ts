/**
 * Re-exported from the neutral `lib/forms/` location (Stage 19), which is
 * now shared with the customer-scoped Sections tab under
 * `app/app/businesses/[businessId]/website/`. Kept here too so this file's
 * existing import path (used by `SectionConfigForm.tsx`,
 * `TestimonialsOrderEditor.tsx`, and their tests) keeps working unchanged.
 */
export { moveSection } from '@/lib/forms/reorder';
