/**
 * Re-exported from the neutral `lib/forms/` location (Stage 19), which is
 * now shared with the customer-scoped equivalents under
 * `app/app/businesses/[businessId]/`. Kept here too so this file's existing
 * import path (used by `actions.ts` and its tests) keeps working unchanged.
 */
export { parseIndexedList } from '@/lib/forms/indexed-list';
